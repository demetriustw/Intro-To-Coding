const btn = document.querySelector("button"); // Get the button from the page
// Detect clicks on the button

const slide = document.querySelector("input");

/*
var clicks = 0;

function btnPressed(clicks) {
  return clicks++;
}

console.log(clicks);
*/
